# Frontend runtime metadata
VITE_APP_NAME="Terminumfrage Tool"
VITE_APP_ENV="local"

# Optional external deployment metadata
VITE_PUBLIC_BASE_URL="http://localhost:5173"

# CI/CD integrations: configure real values as GitHub Actions, Jenkins,
# or local shell secrets. Do not put real tokens in this file.
SONAR_HOST_URL="https://sonarcloud.io"
SONAR_PROJECT_KEY="ffit4-terminumfrage-tool"
SONAR_TOKEN="changeme"

# Static hosting/deployment providers may need a token or site id.
DEPLOY_TARGET="manual-static-artifact"
DEPLOY_TOKEN="changeme"
